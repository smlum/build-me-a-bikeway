{
  "name": "build-me-a-bikeway-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "http-server -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}